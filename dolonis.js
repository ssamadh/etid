let n = 10; // replace with your desired value for n
let m = 5; // replace with your desired value for m

let values0 = new Float32Array(n * m);

// Now you can access and manipulate the elements of the `values0` array
